name: ci

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build-image:
    name: Build container image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          load: true
          push: false
          tags: smol-prometheus:pr-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
        env:
          DOCKER_BUILD_SUMMARY: false
      - name: Run container
        run: |
          docker run -d --rm \
            --name smol-prometheus \
            -p 9090:9090 \
            smol-prometheus:pr-${{ github.sha }}
      - name: Smoke test
        run: |
          set -euo pipefail
          for i in $(seq 1 20); do
            if curl -fsS http://localhost:9090/-/healthy >/dev/null; then
              break
            fi
            sleep 0.5
          done
          curl -fsS http://localhost:9090/graph >/dev/null
          curl -fsS http://localhost:9090/metrics >/dev/null
      - name: Show logs (on failure)
        if: failure()
        run: docker logs smol-prometheus || true
      - name: Stop container
        if: always()
        run: docker stop smol-prometheus || true